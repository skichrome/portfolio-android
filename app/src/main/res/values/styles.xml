<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Toolbar Style -->
    <style name="PortfolioTheme.ToolbarStyle" parent="AppTheme">
        <item name="android:background">@color/colorPrimary</item>
        <item name="titleTextColor">@android:color/white</item>
    </style>

    <!-- Button Style -->
    <style name="PortfolioTheme.ButtonStyle.Default" parent="AppTheme">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
        <item name="android:background">@drawable/btn_radius_primary_color</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:layout_marginBottom">@dimen/buttons_margin</item>
        <item name="android:paddingStart">@dimen/buttons_padding_horizontal</item>
        <item name="android:paddingEnd">@dimen/buttons_padding_horizontal</item>
    </style>

    <!-- FAB Style -->
    <style name="PortfolioTheme.ButtonStyle.FloatingActionButton" parent="AppTheme">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_marginEnd">@dimen/fab_margin</item>
        <item name="android:layout_marginBottom">@dimen/fab_margin</item>
    </style>

    <style name="PortfolioTheme.ButtonStyle.Large" parent="PortfolioTheme.ButtonStyle.Default">
        <item name="android:paddingStart">@dimen/buttons_high_padding_horizontal</item>
        <item name="android:paddingEnd">@dimen/buttons_high_padding_horizontal</item>
    </style>

    <!-- EditTextInputLayout style -->
    <style name="PortfolioTheme.editTextInputLayoutStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">@dimen/tv_input_margin</item>
    </style>

    <!-- EditTextInput style -->
    <style name="PortfolioTheme.editTextInputStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:inputType">text</item>
    </style>
</resources>
